# Autism portal — copy to .env and fill in values
NODE_ENV=development
PORT=3001
PORTAL_SCHEME=http

APPLICANT_HOST=autism.vaidyagogate.org
SCANNER_HOST=scan.autism.vaidyagogate.org
MAIN_SITE_URL=https://autism.vaidyagogate.org

# Use a dedicated DB — not the doctor portal database
# DATABASE_URL=postgresql://user:pass@host:5432/autism_portal

# Optional mail / SMS (same keys as seminar system if shared infra)
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
